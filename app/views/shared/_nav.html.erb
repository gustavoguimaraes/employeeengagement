<header class="navbar navbar-default navbar-static-top orange-nav"role="navigation">
  <div class="container">
    <nav class="collapse navbar-collapse">
      <ul class="nav navbar-nav pull-right">
        <% if user_signed_in? %>
          <p class="navbar-text pull-right"><%= link_to "Log out", logout_path%>
          </p>
        <%else%>
          <p class="navbar-text pull-right">
            <%= link_to "Log in", login_path%>
          </p>
           <p class="navbar-text pull-right">
            <%= link_to "Register", register_path %>
          </p>

        <%end%>
      </ul>
    </nav>
  </div>
</header>
